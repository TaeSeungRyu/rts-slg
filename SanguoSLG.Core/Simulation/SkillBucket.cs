namespace SanguoSLG.Core.Simulation;

/// <summary>패시브 효과가 들어가는 산출 ③ 가산 버킷(design-combat.md). 공격형/방어형.</summary>
public enum SkillBucket
{
    /// <summary>공격 가산 버킷.</summary>
    Attack,

    /// <summary>방어 가산 버킷.</summary>
    Defense,
}
