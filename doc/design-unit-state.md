# 설계 노트 — 부대 상태 시스템: 사기·훈련·군량 (2026-08-13 작성, 계획 초안)

> 전투 축의 마지막 큰 조각. 병력(양)만 있던 부대에 **질(사기·훈련)과 보급(군량)**을 붙인다.
> 피해 공식·행동 규칙은 [design-combat.md](./design-combat.md)·[design-movement.md](./design-movement.md),
> 성 입성/출격·함락은 이동 규칙 7·8과 "함락 처리" 절이 전제다.
> **❓ 표시는 미확정 — 구현 전 사용자 확정 필요.** 확정 수치는 전부 `data/balance.json`으로.

## 왜 셋을 같이 보나

```
훈련도 ──(초기값·저항)──▶ 사기 ──(공/방 보정·패주)──▶ 전투
                          ▲
군량 ──(0이면 급락·이탈)──┘        군량 소모 ◀── 이동·주둔(일수)
```

- **군량**이 기반이다: 일수 기반 소모라 기존 "진행(최대 7일)" 루프에 자연스럽게 붙고,
  사기의 가장 큰 트리거(굶주림)가 된다. 공성전의 **농성·보급 차단**이 여기서 나온다.
- **사기**는 전투 성과·상태의 누적을 공/방 보정과 **패주**로 되돌려주는 되먹임 고리다.
- **훈련도**는 내정(도시)과 전투를 잇는 다리다 — 병력의 질을 만들고, 사기의 바닥을 정한다.

## 구현 순서 (Core 우선 원칙)

### 1단계 — 군량 (기반) ✅ 부대 소모·고갈 구현(2026-08-16)

부대가 군량을 **휴대**하고 야전 일수만큼 소모한다.

| 항목 | 계획 | 상태 |
|---|---|---|
| 휴대량 | 출전 시 성 비축(`City.Provisions`)에서 떼어 감. ❓기본 휴대 일수(예: 30일치) | ❓ |
| 소모 | **병력 비례 × 경과일**. `AdvanceOrchestrator`가 `move.Days`만큼 차감. ❓1만 명 하루 소모량 | ❓ |
| 고갈(0) | 사기 큰 폭 하락(2단계 연동) + ❓병력 이탈률/일(굶주림) | ❓ |
| 보급 | 아군 성 **입성 시 재보급**(성 비축에서 차감). ❓인접 보급 허용 여부 | ❓ |
| 노획 | 시설 약탈 군량도 **휴대 최대치까지만**(초과 소실). 금은 무제한. 입성 시 성에 예치(2026-08-13 확정) | 확정 |
| 수비 군량 | 성도 비축을 소모(수비 병력 비례) → **포위가 길면 성이 먼저 마른다**(농성전) | ❓ |
| 데이터 | `balance.json`: `provisions_per_10k_per_day`, `carry_days_default`, `starvation_desertion_percent` | — |

- Core: `UnitCombatState`(또는 `CombatUnit`)에 `Provisions` 추가, 진행당 차감·고갈 판정.
- 하베스트 케이스: **장기 포위** — 공격군이 성을 포위만 하고(공성 안 함) 수비 군량이 말라가는 것,
  **원정 고갈** — 먼 목표로 행군하다 군량 0 → 이탈 시작.
- 테스트: 소모 계산·고갈 이탈·입성 재보급(성 비축 차감) 검산 고정.

### 2단계 — 사기 (되먹임)

| 항목 | 계획 | 상태 |
|---|---|---|
| 범위 | 0~100. 초기값 = ❓기본 50 + 훈련도 보정(3단계 전까지는 고정 기본값) | ❓ |
| 상승 | 교전 승리(상대보다 덜 깎임) ❓+5 / 적 부대 격파 ❓+10 / 고무(Rally) 스킬 / 성 함락 ❓+15 | ❓ |
| 하락 | 진행당 피해율 비례 ❓ / 아군 부대 전멸 목격(탐지 범위 내) ❓−10 / **군량 0: 일당 ❓−10** / 혼란·교란 피격 ❓−5 | ❓ |
| 효과 | 공/방 배수: ❓`(사기 − 50) × 0.4%` (0이면 −20%, 100이면 +20%) — 기존 배수 층에 한 겹 추가 | ❓ |
| **패주** | 사기 ❓20 미만 → 모드 강제 전환: 명령 불가, **자기 성 방향으로 강제 후퇴**(교란 후퇴 재사용). ❓회복 임계(예: 40)에서 해제 | ❓ |
| 회복 | 성 복귀(입성) 시 ❓전량/일정 회복, 야전 무전투 주둔 시 일당 ❓+2 | ❓ |
| 연동 | 액티브 **고무(Rally)** 를 임시 공/방+10%에서 **실제 사기 +❓20**으로 교체, 패시브 **사기충천**은 사기 하락 저항으로 재정의 검토 | ❓ |

- Core: `CombatUnit.Morale` + 진행 정산 끝에 증감 일괄 적용(결정론 — id 순). 패주는 이동 입력에서
  혼란(행동불가)·교란(강제 후퇴)과 같은 계층으로 처리.
- 하베스트 케이스: **소모전 사기 붕괴** — 대등 전투가 길어지다 한쪽이 임계 밑으로 → 패주 후퇴,
  **고무 반격** — 패주 직전 부대를 고무로 붙잡는 것.
- 테스트: 증감 트리거별 검산, 패주 전환·해제, 공/방 배수 층.

### 3단계 — 훈련도 + 병사 모집 (질·수급)

**내정 전체가 아니라 최소 조각만 앞당긴다(2026-08-13 결정)**: 모병·징병 명령 + 금 차감 +
도시 대기 부대의 훈련 틱 + 투입 가능 판정. 세금·개발·치안 등 나머지 내정은 별도.

#### 모집 (2026-08-13 사용자 확정)

| | 모병 | 징병 |
|---|---|---|
| 비용 | **금 소모** ❓수량당 비용 | **무료 — 대신 도시 치안 대폭 하락** ❓하락 폭 |
| 초기 훈련도 | **50** | **0** |
| 전쟁 투입 | **즉시 가능** | **불가 — 훈련도 50 도달 시 해제**(7일 +10이라 35일) |
| 훈련 | 바로 훈련 가능(도시 훈련 명령) | 훈련 명령 불가 — **7일 단위 +10 자동 상승만** |

- 치안(Security)은 이 조각에서는 **수치 필드만** 둔다(징병이 깎는 대상). 치안이 낳는
  효과(반란·수입 감소 등)와 회복 수단은 내정 본편에서 설계한다.
- **생산 자원(2026-08-13 확정)**: 모병·징병 공통으로 **광석**이 들고, **기병은 말**,
  **상병은 코끼리**가 추가로 든다 — 자원이 없으면 그 병종은 생산 불가.
  소모량·산출은 [design-administration.md](./design-administration.md) ❓7.

- **보충 희석**: 기존 부대에 병력을 합치면 훈련도는 **가중 평균, 반올림**
  (예: 훈련 80 × 6000 + 신병 50 × 4000 = 68).
- 하베스트/샌드박스 케이스: 징병 부대가 35일 뒤 투입 가능해지는 것, 모병 즉시 투입,
  보충 희석 반올림 검산.

#### 훈련도 효과

| 항목 | 계획 | 상태 |
|---|---|---|
| 범위 | 0~100 | 확정 |
| 효과 | ❓A안: 공/방 배수 한 겹 (`(훈련 − 50) × 0.2%`) / ❓B안: 사기 초기값·하락 저항으로만 작용(전투 배수는 사기에 일임) | ❓ |
| 전투 성장 | 교전한 진행당 ❓+1 (실전 경험, 상한 ❓80 — 도시 훈련으로만 그 위) | ❓ |
| 도시 훈련 명령 | 모병 부대만 가능. ❓상승량/주기 | ❓ |
| 데이터 | 병종별 훈련 상한/속도 차이는 두지 않는다(단순 유지) ❓ | ❓ |

## 원칙 (전 단계 공통)

- 수치는 전부 `data/balance.json` — 코드 하드코딩 금지 (CLAUDE.md 규칙 3)
- 모든 증감은 진행 정산 안에서 id 오름차순 일괄 적용 — 결정론 (규칙 4)
- 각 단계마다: Core + xUnit 검산 → 하베스트 케이스 → Sandbox 대량 진행으로 수렴 확인
- 문서 갱신: 확정된 규칙은 이 문서의 ❓를 지우고 값을 박은 뒤, 효과가 닿는
  design-combat(전투 배수 층)·design-movement(패주 이동)·design-skill-actives(고무)에도 반영

## 미확정 질문 요약 (구현 전 확정 필요)

1. **군량**: 휴대 일수 기본값? 1만 명 하루 소모량? 고갈 시 이탈률? 인접 보급 허용?
2. **사기**: 증감 표의 수치들? 공/방 보정 폭(0~100 → ±20%?)? 패주 임계/해제 값? 패주 중 피격 규칙?
3. **훈련도**: 전투 배수로 쓸지(A) 사기 쪽으로만 쓸지(B)? 신병 초기값·성장 속도?
4. **순서**: 군량 → 사기 → 훈련 순서로 진행해도 좋은가?
